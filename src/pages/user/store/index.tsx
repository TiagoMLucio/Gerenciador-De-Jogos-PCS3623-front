import { NextPage } from 'next';
import React from 'react';
import UserStoreTemplate from 'template/Store';

const UserStore: NextPage = () => {
    return <UserStoreTemplate />;
};

export default UserStore;
